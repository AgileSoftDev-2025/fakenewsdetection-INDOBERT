# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# File Processing
python-multipart==0.0.17
pytesseract==0.3.13
python-docx==1.1.2
Pillow>=8.0.0

# HTTP Client (for HF Space API calls)
httpx>=0.24.0

# Data Processing (minimal)
pandas>=2.0.0

# Utilities
python-dotenv>=1.0.0

# ======================================
# REMOVED FOR PRODUCTION DEPLOYMENT:
# ======================================
# torch>=2.0.0                  # 900 MB - Only needed for local training
# transformers>=4.30.0          # Only needed for local inference
# scikit-learn>=1.3.0           # Not used in backend API
# numpy (pandas dependency)     # Will be installed automatically
# huggingface-hub>=0.18.1       # Only needed for model upload (local training)
